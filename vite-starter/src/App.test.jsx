import { render, screen } from '@testing-library/react';
import App from './App';

test('초기 버튼 색깔', () => {});
test('초기 버튼 텍스트', () => {});
test('클릭 후 버튼 색깔', () => {});
test('클룩 후 버튼 텍스트', () => {});
